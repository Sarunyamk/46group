/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'plus.unsplash.com',
      'images.unsplash.com',
      'media.istockphoto.com'
    ],
  },
};

module.exports = nextConfig;
